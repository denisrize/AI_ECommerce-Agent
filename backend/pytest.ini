[pytest]
# auto mode means you don't need @pytest.mark.asyncio on every test
# if all your tests are async (which ours are)
asyncio_mode = auto
testpaths = tests
python_files = test_*.py
python_functions = test_*
# -v = verbose output (shows each test name and result)
addopts = -v
